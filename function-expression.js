var area = function(width, height) {					// anonymous function, stored in a variable
	return width * height;								// what's returned to the caller
}

var size = area(3, 4);									// function gets called and stored in another variable 